/*find square root of a number*/
#include<stdio.h>

int main()
{
    double n, result;
    printf("Enter the number: ");
    scanf("%lf",&n);

    //result=sqrt(n);
    printf("square root of %lf is %lf",n,sqrt(n));
    return 0;
}